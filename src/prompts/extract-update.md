You are updating a technical context document for the Grass content factory.

Here is the current context:
{{existing_context}}

Here are the code changes since the last extraction (git diff):
{{git_diff}}

Update the context document to reflect these changes. Add new features, update changed behaviors, remove references to deleted functionality. Keep the same structure and level of detail. Only modify sections affected by the diff.

Return the complete updated context document.
